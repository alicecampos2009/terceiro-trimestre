let numeros = [1, 2, 3];
numeros.push(4,5); //adiciona item no final do array
console.log(numeros);

let numerais = [1, 2]
numeros.unshifit(0); //adiciona no inicio do array
numeros.log(numerais);

let nomes = ("carlos", "Ana", "Bruno")
nomes.sort(); // Organiza os elementos em ordem
console.log(nomes);